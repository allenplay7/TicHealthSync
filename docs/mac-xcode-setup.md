# Mac Xcode Setup

Not needed for Phase 1.

Later phases will add a SwiftUI iOS app under `ios-app/`. That code can be copied to a Mac and opened in Xcode for CoreBluetooth and HealthKit development.

